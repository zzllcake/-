# ✅ 代码审查系统验证教程

> 一步步验证你的自动代码审查系统是否完整可用

---

## 📋 验证清单（6 项）

| # | 验证项 | 命令/操作 | 预期结果 |
|---|--------|-----------|----------|
| 1 | 本地检查链 | `npm run check:all` | 全部通过 |
| 2 | 工作流配置 | `gh workflow list` | 显示 active |
| 3 | 分支保护 | `gh api repos/.../protection` | 已启用 |
| 4 | 实际触发审查 | 创建 PR | Actions 自动运行 |
| 5 | 错误捕获 | 提交错误代码 | 检查失败 + 评论 |
| 6 | 审查历史 | 网页控制台 | 显示所有运行 |

---

## 🔬 验证 1：本地检查链

在项目目录打开 Git Bash：

```bash
cd /d/ZCodeProject
npm run check:all
```

**预期输出：**

```
> eslint src/ --ext .ts,.tsx,.js,.jsx
✅ 0 errors, 0 warnings

> prettier --check "src/**/*.{ts,tsx,js,jsx,json,css,md}"
✅ All matched files use Prettier code style!

> tsc --noEmit
✅ 无输出（类型安全）

> vitest run
✅ 65 tests passed

> 覆盖率
✅ 100% statements / branches / functions / lines
```

**如果失败：**

```bash
npm run lint:fix    # 自动修 ESLint
npm run format      # 自动格式化
```

---

## 🔬 验证 2：工作流配置

```bash
gh workflow list
```

**预期输出：**

```
🤖 代码审查  active
```

**检查详细配置：**

```bash
gh workflow view "🤖 代码审查"
```

**应该看到 5 个 Job：**

```
🔍 代码规范检查
🔷 类型安全检查
🧪 测试 (Node 18)
🧪 测试 (Node 20)
🧪 测试 (Node 22)
🛡️ 安全扫描
📝 审查汇总
```

---

## 🔬 验证 3：分支保护

```bash
gh api repos/zzllcake/-/branches/main/protection
```

**预期输出包含：**

```json
{
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "🔍 代码规范检查",
      "🔷 类型安全检查",
      "🛡️ 安全扫描"
    ]
  },
  "allow_force_pushes": { "enabled": false },
  "allow_deletions": { "enabled": false }
}
```

**这意味着：** 检查不通过时无法合并 PR ✅

---

## 🔬 验证 4：实际触发审查

### 步骤 1：创建测试分支

```bash
git checkout main
git pull
git checkout -b test/verify-review
```

### 步骤 2：做一个小修改

打开 `src/utils/greet.ts`，在末尾加一行注释：

```typescript
// 验证测试
```

### 步骤 3：本地验证并提交

```bash
npm run check:all
git add .
git commit -m "test: 验证自动审查"
git push -u origin test/verify-review
```

### 步骤 4：创建 PR

```bash
gh pr create --title "验证自动审查" --body "测试自动审查流程"
```

**预期：** 命令返回 PR 链接

### 步骤 5：观察审查自动运行

```bash
# 等待几秒
sleep 10
gh pr checks
```

**预期输出：**

```
🔍 代码规范检查      pass    15s
🔷 类型安全检查      pass    12s
🧪 测试 (Node 18)    pass    20s
🧪 测试 (Node 20)    pass    18s
🧪 测试 (Node 22)    pass    19s
🛡️ 安全扫描          pass    1m5s
📝 审查汇总          pass    6s
```

### 步骤 6：查看详情

```bash
gh run view --log | head -50
```

或者去浏览器看：
```
https://github.com/zzllcake/-/pull/你的PR号
```

---

## 🔬 验证 5：错误捕获测试

### 步骤 1：故意写错误代码

创建 `src/bad-code.ts`：

```typescript
// 错误 1：未使用变量
const unusedVariable = "test";

// 错误 2：any 类型
function processData(data: any): any {
  return data;
}

// 错误 3：字符串拼接
function hello(name: string) {
  return "Hello " + name;
}

// 错误 4：== 而非 ===
function compare(a: number, b: number) {
  return a == b;
}
```

### 步骤 2：本地验证（应该失败）

```bash
npm run lint
```

**预期输出：**

```
src/bad-code.ts
   2:7   error  'unusedVariable' is assigned a value but never used  @typescript-eslint/no-unused-vars
   5:24  error  Unexpected any. Specify a different type              @typescript-eslint/no-explicit-any
  11:10  error  Expected '===' and instead saw '=='                  eqeqeq

✖ 3 problems (3 errors, 0 warnings)
```

### 步骤 3：提交并推送

```bash
git add src/bad-code.ts
git commit -m "test: 测试错误捕获"
git push
```

### 步骤 4：观察审查结果

```bash
sleep 60
gh pr checks
```

**预期输出：**

```
🔍 代码规范检查      fail    15s    ← 应该有错误
🔷 类型安全检查      fail    12s    ← 应该有错误
🧪 测试              pass    ...
```

### 步骤 5：查看 PR 评论

```bash
gh pr view --comments
```

**预期：** 🤖 机器人发布的评论

```
## 🤖 自动代码审查结果

| 检查项 | 状态 |
|--------|------|
| ❌ 🔍 代码规范检查 | **失败** |
| ❌ 🔷 类型安全检查 | **失败** |

### ❌ 失败的检查项
- [:x: 🔍 代码规范检查](链接)
- [:x: 🔷 类型安全检查](链接)
```

### 步骤 6：点击评论中的链接

可以看到具体错误：

```
src/bad-code.ts:2:7
'✖ 3 problems (3 errors, 0 warnings)'
```

---

## 🔬 验证 6：审查历史（网页控制台）

### 启动控制台

双击 `D:\ZCodeProject\启动控制台.bat`

或者命令行：

```bash
cd /d/ZCodeProject
node server.cjs
```

浏览器打开：**http://localhost:3456**

### 应该看到

**左侧：** 所有分支和运行记录

```
📂 分支 / 运行
├─ #5 challenge/500-errors     ❌ 失败 3次运行
├─ #4 feat/new-feature         ✅ 通过 1次运行
├─ #3 fix/review-issue         ❌ 失败 2次运行
└─ main                        ✅ 通过 5次运行
```

**右侧：** 点击分支后显示错误详情

```
📋 challenge/500-errors

🔴 128 错误  🟡 5 警告

📁 src/error-types-20000.ts
  ❌ 位置: 19011:3   规则: parsing-error
     Expression expected

  ⚠️ 位置: 15002:10  规则: no-unused-vars
     'e01' is defined but never used
```

---

## 🎯 验证流程图

```
┌─────────────────────────────────────────────────────┐
│  验证 1: 本地检查链                                  │
│  npm run check:all → 全部通过 ✅                     │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│  验证 2: 工作流配置                                  │
│  gh workflow list → active ✅                       │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│  验证 3: 分支保护                                    │
│  gh api .../protection → 已启用 ✅                   │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│  验证 4: 创建测试 PR                                 │
│  git push → 创建 PR → 审查自动运行 ✅                │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│  验证 5: 错误捕获                                    │
│  故意写错 → 检查失败 + 机器人评论 ✅                 │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│  验证 6: 网页控制台                                  │
│  双击启动控制台.bat → 查看所有历史记录 ✅            │
└─────────────────────────────────────────────────────┘
```

---

## 🚨 常见问题排查

### 问题 1：工作流没触发

**检查：**

```bash
gh run list --limit 5
```

**可能原因：**
- PR 目标分支不是 main
- Actions 被禁用

**修复：**

```bash
gh api repos/zzllcake/-/actions/permissions --method PUT -f enabled=true
```

### 问题 2：检查全部通过但代码有问题

**检查：**

```bash
cat .eslintrc.cjs | grep "error"
```

**可能原因：** 该错误类型没有配置对应规则

**修复：** 在 `.eslintrc.cjs` 的 `rules` 中添加规则

### 问题 3：网页打不开

**检查：**

```bash
curl http://localhost:3456/api/status
```

**可能原因：** 服务器没启动

**修复：**

```bash
node server.cjs
```

### 问题 4：无法合并 PR

**这可能是正常的！** 检查是否通过：

```bash
gh pr checks
```

如果有 ❌，先修复代码。

---

## 📊 验证结果记录表

| # | 验证项 | 命令 | 结果 |
|---|--------|------|------|
| 1 | 本地检查链 | `npm run check:all` | ☐ 通过 / ☐ 失败 |
| 2 | 工作流配置 | `gh workflow list` | ☐ active / ☐ 未配置 |
| 3 | 分支保护 | `gh api .../protection` | ☐ 已启用 / ☐ 未启用 |
| 4 | 触发审查 | 创建 PR | ☐ 自动运行 / ☐ 未触发 |
| 5 | 错误捕获 | 提交错误代码 | ☐ 捕获成功 / ☐ 漏检 |
| 6 | 网页控制台 | `启动控制台.bat` | ☐ 正常 / ☐ 异常 |

---

## ✨ 一键验证脚本

创建 `verify.sh` 并运行：

```bash
#!/usr/bin/env bash
echo "🔍 代码审查系统验证"
echo "===================="

echo ""
echo "[1/6] 本地检查链..."
npm run check:all > /dev/null 2>&1 && echo "  ✅ 通过" || echo "  ❌ 失败"

echo ""
echo "[2/6] 工作流状态..."
gh workflow list 2>/dev/null | grep -q "active" && echo "  ✅ 已启用" || echo "  ❌ 未启用"

echo ""
echo "[3/6] 分支保护..."
gh api repos/zzllcake/-/branches/main/protection > /dev/null 2>&1 && echo "  ✅ 已配置" || echo "  ❌ 未配置"

echo ""
echo "[4/6] 最近运行..."
COUNT=$(gh run list --limit 10 --json id 2>/dev/null | grep -c '"id"' || echo 0)
echo "  📊 最近运行: $COUNT 次"

echo ""
echo "[5/6] 覆盖率..."
npm run test:coverage 2>&1 | grep -q "100" && echo "  ✅ 100%" || echo "  ⚠️ 未达标"

echo ""
echo "[6/6] 网页服务器..."
curl -s http://localhost:3456/api/status > /dev/null 2>&1 && echo "  ✅ 运行中" || echo "  ⚠️ 未启动"

echo ""
echo "===================="
echo "验证完成！"
```

**使用方法：**

```bash
bash verify.sh
```

---

## 📚 相关文档

- [README.md](./README.md) - 项目说明
- [.github/workflows/code-review.yml](./.github/workflows/code-review.yml) - 工作流配置
- [.eslintrc.cjs](./.eslintrc.cjs) - ESLint 规则

---

**验证完成后，你的系统就可以放心使用了！** 🎉
